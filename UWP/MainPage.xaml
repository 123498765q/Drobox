<Page
    x:Class="UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <ScrollViewer>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <SplitView IsPaneOpen="True"
           DisplayMode="Inline"
           OpenPaneLength="120">
                <SplitView.Pane>
                    
                        <RelativePanel x:Name="RP">
                        
                        </RelativePanel>
                    
                </SplitView.Pane>

                <Grid>
                    <RelativePanel x:Name="CP">

                    </RelativePanel>
                </Grid>
            </SplitView>
            <Button Content="Button" HorizontalAlignment="Left" Margin="588,273,0,0" VerticalAlignment="Top" Click="Button_Click" RenderTransformOrigin="0.5,0.5" Height="117" UseLayoutRounding="False" Width="194" d:LayoutRounding="Auto">
            <Button.RenderTransform>
                <CompositeTransform Rotation="-29.93"/>
            </Button.RenderTransform>
        </Button>
        </Grid>
    </ScrollViewer>

</Page>
